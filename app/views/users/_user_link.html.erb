<div class="row user-link-hidden-toggle
  <%= class_hidden user.user_links %>">
  <div class="panel panel-default user-info-panel">
    <div class="user-link">
      <strong class="user-link-title">
        <%= t ".links" %>
      </strong>
      <% if user.is_user? current_user %>
        <div class="edit-profile-button">
          <div class="btn-group">
            <%= link_to new_user_link_path, remote: true,
              class: "btn btn-default btn-sm tip btn-responsive btn-add-user-link" do %>
              <span class="icon-plus-sign"></span>
              <%= t ".add" %>
            <% end %>
            <button type="button"
              class="btn btn-default btn-sm tip btn-responsive dropdown-toggle"
              data-toggle="dropdown" aria-expanded="true">
              <%= information_status user, "link" %></span> <span class="caret"></span>
            </button>
            <ul class="dropdown-menu" role="menu">
              <li>
                <%= link_to "javascript:;", class: "edit-info-status",
                  data: {status: InfoUser::INFO_STATUS[:only_me],
                    info: "link", id: current_user.info_user.id} do %>
                  <span class="icon-lock"> <%= t ".only_me" %></span>
                <% end %>
              </li>
              <li>
                <%= link_to "javascript:;", class: "edit-info-status",
                  data: {status: InfoUser::INFO_STATUS[:public],
                    info: "link", id: current_user.info_user.id} do %>
                  <span class="icon-globe"> <%= t ".public" %></span>
                <% end %>
              </li>
            </ul>
          </div>
        </div>
      <% end %>
      <hr>
      <div class="list-user-link">
        <%= render partial: "user_links/user_link", collection: user.user_links.newest %>
      </div>
    </div>
  </div>
</div>
