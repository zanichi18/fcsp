$('.user-work').html('<%= j render "user_works/user_work", user: current_user %>');
$('#user-work-form').modal('hide');
<% if @render.status == 200 %>
  $.growl.notice({message: '<%= @render.message %>'});
<% else %>
  $.growl.error({message: '<%= @render.message %>'});
<% end %>
